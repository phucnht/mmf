import { Profile } from 'views/Profile';
import { PublicLayout } from 'layouts';

const Home = () => {
  return (
    <PublicLayout>
      <Profile />
    </PublicLayout>
  );
};

export default Home;
